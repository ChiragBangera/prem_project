services:
  - type: web
    name: prem-analytics-api
    runtime: python
    plan: free
    buildCommand: pip install .
    startCommand: uvicorn app.api:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /health
    autoDeploy: true
